#!/usr/bin/env node

var jr = require('../');

jr.run({
  jobNames: process.argv.slice(2)
}, function (err) {
  if (err) {
    console.log('[jr] ' + err);
  } else {
    console.log('[jr] Done');
  }
});